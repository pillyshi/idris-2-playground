# 2026-02-08

## やったこと
- 代数的構造（群）の形式化の継続（`practices/practice_20260208.idr`）
- Law定義をすべてinfix記法（`` u `p` v ``）に統一（`Commutative`, `LeftNeutral`, `RightNeutral`, `LeftInverse`, `RightInverse`）
- 中間的な代数的構造（`SemiGroup`, `CommutativeSemiGroup`, `Monoid`, `CommutativeMonoid`）を削除し、`Group`に集中する構成に簡素化
- `rightInverse` 補題を追加（`LeftInverse` と `Commutative` から導出）
- `solveInverseLeft` の型を変更: `u `p` v === z -> u === i v` に修正し、証明を書き直し

## 学んだこと
- infix記法（`` u `p` v ``）に統一することで `Calc` ブロック内の証明ステップがより読みやすくなる
- `solveInverseLeft` の新しい証明では `rightNeutral` → `rightInverse` → `associative` → `leftNeutral` の順で変形
  - 前日版（`v `p` u === z -> u === i v`）とは引数の順序と使う補題の組み合わせが異なる
- 構造を段階的に定義するアプローチ vs 必要な構造だけに絞るアプローチのトレードオフ

## 次にやること
- `invertProduct` の証明
- `rightInjective`, `solveInverseRight`, `inverseZero`, `inverseInvolutory` の実装
- `AbelianGroup`（アーベル群）の定義
