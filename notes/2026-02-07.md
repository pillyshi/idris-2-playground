# 2026-02-07

## やったこと
- 代数的構造（群）の形式化の継続（`practices/practice_20260207.idr`）
- Law定義の記法をinfix記法からprefix記法に変更
- `solveInverseLeft` の証明を実装
- `invertProduct` に着手（未完了）

## 学んだこと
- `Calc`ブロックで `..<` を使うと等式の向きを反転できる（右辺から左辺への書き換え）
- 群の左消去法則（`leftInjective`）を使って方程式を解く流れ
- `solveInverseLeft`: `v `p` u === z` から `u === i v` を導出する手順
  - leftNeutral, leftInverse, associative を組み合わせて変形

## 次にやること
- `invertProduct` の証明
- `rightInjective`, `solveInverseRight`, `inverseZero`, `inverseInvolutory` の実装
- `AbelianGroup` (アーベル群) の定義
